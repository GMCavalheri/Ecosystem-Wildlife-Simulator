#include "core/MetricsRecorder.h"

namespace eco {

void MetricsRecorder::snapshot(const entt::registry& /*registry*/) {
    // Phase 1: record population counts per tick to CSV for validation plotting.
}

} // namespace eco
